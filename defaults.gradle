if (!hasProperty("shiroConfigurationLocations")) {
  ext.shiroConfigurationLocations = "/WEB-INF/shiro.ini";
}

if (!hasProperty("stagemonitor.active")) {
  ext["stagemonitor.active"] = "false";
}

if (!hasProperty("wildflyHostname")) {
  ext.wildflyHostname = "localhost";
}

if (!hasProperty("wildflyUsername")) {
  ext.wildflyUsername = "admin";
}

if (!hasProperty("wildflyPassword")) {
  ext.wildflyPassword = "megapassword";
}

if (!hasProperty("wildflyAdminPort")) {
  ext.wildflyAdminPort = "9990";
}

if (!hasProperty("wildflyPort")) {
  ext.wildflyPort = "8080";
}

if (!hasProperty("shiro.user.username")) {
  ext["shiro.user.username"] = "user";
}

if (!hasProperty("shiro.user.password")) {
  ext["shiro.user.password"] = "user";
}

if (!hasProperty("shiro.admin.username")) {
  ext["shiro.admin.username"] = "admin";
}

if (!hasProperty("shiro.admin.password")) {
  ext["shiro.admin.password"] = "admin";
}
